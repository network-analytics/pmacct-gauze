[package]
name = "pmacct-gauze-lib"
version = "0.1.0"
edition = "2021"

build = "build.rs"

[build-dependencies]
pmacct-gauze-bindings = { path = "../pmacct-gauze-bindings", features = ["export-renames"] }
toml = "0.8.2"
toml_edit = "0.20.2"
if_chain = "1.0.2"

[dependencies]
libc = "0.2.148"
netgauze-parse-utils = "0.1.0"
netgauze-bmp-pkt = "0.1.0"
nom = "7.1.3"
pmacct-gauze-bindings = { path = "../pmacct-gauze-bindings" }

[features]
capi = []